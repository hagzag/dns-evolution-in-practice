options {
  directory "/var/cache/bind";
  recursion no;
  allow-query { any; };
  listen-on { any; };
  listen-on-v6 { any; };
};

zone "lab.dns" IN {
  type master;
  file "/etc/bind/zones/db.lab.dns";
};
